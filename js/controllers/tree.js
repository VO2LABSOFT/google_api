'use strict';

/**
 * Tree Controller.
 * Render files/folder.
 */
mainApp.controller('TreeController', ['$scope', '$rootScope', 'gdisk', '$mdDialog', '$mdToast',
        function($scope, $rootScope, gdisk, $mdDialog, $mdToast) {

            this.name = 'TreeController';

        }]
);